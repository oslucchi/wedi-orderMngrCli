import { MatInputNumericDirective } from './mat-input-numeric.directive';

describe('MatInputNumericDirective', () => {
  it('should create an instance', () => {
    const directive = new MatInputNumericDirective();
    expect(directive).toBeTruthy();
  });
});
